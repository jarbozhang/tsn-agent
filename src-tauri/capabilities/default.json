{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main TSN Agent window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
