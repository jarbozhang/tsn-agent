# U0 基线测量与残值估算（refactor/app-modularization-2026-06-02）

实施期辅助文档；本 PR merge 前删除。

## 测试基线

`npx vitest run src/app/App.test.tsx --reporter=verbose` (2026-06-02)

**Pass 总数：24**。完整列表：

1. does not overwrite the visible project when an agent result is marked non-applicable
2. shows a waiting animation before the first streaming chunk arrives
3. scrolls the chat to the newest streamed output
4. keeps a global running indicator visible while streamed replies wait for long tasks
5. exposes a project export action after artifacts are generated
6. runs planner task from running to succeeded and adds result artifacts
7. does not create planner output when result endpoint returns a business error
8. invalidates planner result artifacts when the project changes
9. keeps polling after a transient planner status failure
10. prevents an in-flight poll from writing results after the user stops a planner task
11. highlights flow route nodes and links when selecting a flow row
12. uses the selected flow route direction for highlighted edges
13. marks the final planning stage confirmed instead of rerunning it
14. updates topology change requests using the target switch count
15. keeps per-switch host edits and renders ring interconnect requests
16. advances from topology to time sync using natural language stage intent
17. switches inspector tabs and opens node and link details from topology clicks
18. re-enables submit if pending session persistence fails
19. falls back to an in-memory initial session if startup persistence fails
20. does not restore a session deleted while the agent is running
21. opens workspace skill and settings panels from the tool rail
22-24. (其他 3 个 — 实施期间核对完整 verbose 输出)

每抽一个 hook 后跑 `npx vitest run src/app/App.test.tsx --reporter=verbose`，**这 24 个 test 名必须全部仍 pass**。

## App.tsx 行数分布（U0 时）

总 2400 行（含 import 和空行）。

| 段 | 行号区间 | 估算 |
|---|---|---|
| import + 顶层常量 | 1-118 | 118 行 |
| 顶层 useState / useRef | 119-141 | 23 行 |
| useEffect 集中区 (8 个) | 143-268, 373-393 | ~146 行 |
| 派生数据块 + useMemo | 270-371 | 102 行 |
| 持久化辅助 (persistSession/PlannerSession) | 395-417 | 23 行 |
| submitIntent 主流程 | 419-608 | 190 行 |
| sessionExists / updateAssistantMessage | 610-627 | 18 行 |
| Session CRUD handlers | 629-696 | 68 行 |
| refreshBundle | 698-720 | 23 行 |
| Planner action handlers | 722-847 | 126 行 |
| Planner 轮询 + 取消 | 849-928 | 80 行 |
| attachPlannerResult | 930-971 | 42 行 |
| Export handlers | 973-1033 | 61 行 |
| 拓扑选中 handlers | 1035-1047 | 13 行 |
| 顶层 JSX 返回 | 1049-1486 | 438 行 |
| 子组件定义（WorkspaceToolRail / ToolDrawer / panels / TsnTopologyNode / Step / AgentWaitingIndicator / Stat / DetailRow / AgentStepSummaryGroup / AgentStepDetailView / LegacyOriginBanner / AgentRunStatusBar / PlannerTaskPanel / ReleaseNoteDetail）+ 文案常量 + 派生纯函数（bundleForAgentResult / plannerRunForAgentResult / 等 ~14 个） | 1488-2400 | 913 行 |

## 残值估算

| 抽出 unit | 估算减少 | App.tsx 残余 |
|---|---|---|
| U1 useSessionRepository（hydration effect + 4 handlers + persistSession 部分） | -200 | ~2200 |
| U2 useAgentRunController（3 effect + 6 state + submitIntent + updateAssistantMessage） | -340 | ~1860 |
| U3 usePlannerRun（baseUrl/state + 同步 effect + start/stop/poll/attach + persistPlannerSession + 8 个纯函数） | -370 | ~1490 |
| U4 useProjectExport（3 state + 建议目录 effect + 3 handlers + refreshBundle + bundleForAgentResult） | -160 | ~1330 |
| U5 WorkspaceTools 一组（5 个组件 + labels + helpers） | -450 | ~880 |
| U6 ChatPane + AgentStepStream + AgentRunStatusBar + 文案常量 | -320 | ~560 |
| U7 WorkspacePane + TopologyStage + 6 panel + Stat/DetailRow/findPortIndex/groupArtifacts/TsnTopologyNode/PlannerTaskPanel + 工具常量 | -420 | ~140 |
| **U11 收尾** 添加 | imports + 顶层 layout JSX (~80) + hook composition + 残余 派生 + WorkspaceDrawer 状态 | ~300 |

**预计 App.tsx 收尾后 ~300 行，落在 ≤400 budget 内。**

兜底方案（若实测 >400）：
- 抽 `useTopologyDerivations` hook 把 line 270-371 派生块的 flowTopology / selectedNode / selectedLink / switchCount/endSystemCount/linkCount/flowCount 全部转移
- 或把 imports 中的多重 type re-export 减少（重叠 import 合并）

## U2 后 checkpoint 度量

U2 完成后，进入 U3 前必须度量：

- `wc -l src/app/App.tsx`（预期 ~1860）
- vitest 全套耗时（基线对比，预期增加 < 20%）
- App.test.tsx 24 个 test 名全绿

如果 App.tsx 残余 >2000 行，说明 U1/U2 抽出量不足，需重新评估 hook 范围。
